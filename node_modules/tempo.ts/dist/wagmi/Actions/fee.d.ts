import type * as Query from '@tanstack/query-core';
import { type Config } from '@wagmi/core';
import type { ChainIdParameter, ConnectorParameter } from '@wagmi/core/internal';
import type { Account } from 'viem';
import type { PartialBy, RequiredBy, UnionOmit } from '../../internal/types.js';
import * as viem_Actions from '../../viem/Actions/fee.js';
/**
 * Gets the user's default fee token.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const hash = await Actions.fee.getUserToken(config, {
 *   account: '0x20c...0055',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns Transaction hash.
 */
export declare function getUserToken<config extends Config>(config: config, parameters: getUserToken.Parameters<config>): Promise<getUserToken.ReturnValue>;
export declare namespace getUserToken {
    type Parameters<config extends Config> = ChainIdParameter<config> & viem_Actions.getUserToken.Parameters;
    type ReturnValue = viem_Actions.getUserToken.ReturnValue;
    function queryKey<config extends Config>(parameters: PartialBy<Parameters<config>, 'account'>): readonly ["getUserToken", PartialBy<Parameters<config>, "account">];
    type QueryKey<config extends Config> = ReturnType<typeof queryKey<config>>;
    function queryOptions<config extends Config, selectData = ReturnValue>(config: Config, parameters: queryOptions.Parameters<config, selectData>): queryOptions.ReturnValue<config, selectData>;
    namespace queryOptions {
        type Parameters<config extends Config, selectData = getUserToken.ReturnValue> = PartialBy<getUserToken.Parameters<config>, 'account'> & {
            query?: Omit<ReturnValue<config, selectData>, 'queryKey' | 'queryFn'> | undefined;
        };
        type ReturnValue<config extends Config, selectData = getUserToken.ReturnValue> = RequiredBy<Query.QueryOptions<getUserToken.ReturnValue, Query.DefaultError, selectData, getUserToken.QueryKey<config>>, 'queryKey' | 'queryFn'>;
    }
}
/**
 * Sets the user's default fee token.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const result = await Actions.fee.setUserToken(config, {
 *   token: '0x20c...0055',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction receipt and event data.
 */
export declare function setUserToken<config extends Config>(config: config, parameters: setUserToken.Parameters<config>): Promise<viem_Actions.setUserToken.ReturnValue>;
export declare namespace setUserToken {
    type Parameters<config extends Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.setUserToken.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.setUserToken.ReturnValue;
    type ErrorType = viem_Actions.setUserToken.ErrorType;
}
/**
 * Sets the user's default fee token.
 *
 * Note: This is a synchronous action that waits for the transaction to
 * be included on a block before returning a response.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const result = await Actions.fee.setUserTokenSync(config, {
 *   token: '0x20c...0055',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction receipt and event data.
 */
export declare function setUserTokenSync<config extends Config>(config: config, parameters: setUserTokenSync.Parameters<config>): Promise<viem_Actions.setUserTokenSync.ReturnValue>;
export declare namespace setUserTokenSync {
    type Parameters<config extends Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.setUserTokenSync.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.setUserTokenSync.ReturnValue;
    type ErrorType = viem_Actions.setUserTokenSync.ErrorType;
}
//# sourceMappingURL=fee.d.ts.map