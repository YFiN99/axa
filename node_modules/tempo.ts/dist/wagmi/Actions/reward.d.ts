import type * as Query from '@tanstack/query-core';
import { type Config } from '@wagmi/core';
import type { ChainIdParameter, ConnectorParameter } from '@wagmi/core/internal';
import type { Account } from 'viem';
import type { RequiredBy, UnionOmit } from '../../internal/types.js';
import * as viem_Actions from '../../viem/Actions/reward.js';
/**
 * Claims accumulated rewards for a recipient.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const hash = await Actions.reward.claim(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction hash.
 */
export declare function claim<config extends Config>(config: config, parameters: claim.Parameters<config>): Promise<viem_Actions.claim.ReturnValue>;
export declare namespace claim {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.claim.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.claim.ReturnValue;
    type ErrorType = viem_Actions.claim.ErrorType;
}
/**
 * Claims accumulated rewards for a recipient and waits for confirmation.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const result = await Actions.reward.claimSync(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction receipt.
 */
export declare function claimSync<config extends Config>(config: config, parameters: claimSync.Parameters<config>): Promise<viem_Actions.claimSync.ReturnValue>;
export declare namespace claimSync {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.claimSync.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.claimSync.ReturnValue;
    type ErrorType = viem_Actions.claimSync.ErrorType;
}
/**
 * Gets the total reward per second rate for all active streams.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const rate = await Actions.reward.getTotalPerSecond(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The total reward per second (scaled by 1e18).
 */
export declare function getTotalPerSecond<config extends Config>(config: config, parameters: getTotalPerSecond.Parameters<config>): Promise<bigint>;
export declare namespace getTotalPerSecond {
    type Parameters<config extends Config> = ChainIdParameter<config> & viem_Actions.getTotalPerSecond.Parameters;
    type ReturnValue = viem_Actions.getTotalPerSecond.ReturnValue;
    function queryKey<config extends Config>(parameters: Parameters<config>): readonly ["getTotalPerSecond", Parameters<config>];
    type QueryKey<config extends Config> = ReturnType<typeof queryKey<config>>;
    function queryOptions<config extends Config, selectData = ReturnValue>(config: Config, parameters: queryOptions.Parameters<config, selectData>): queryOptions.ReturnValue<config, selectData>;
    namespace queryOptions {
        type Parameters<config extends Config, selectData = getTotalPerSecond.ReturnValue> = getTotalPerSecond.Parameters<config> & {
            query?: Omit<ReturnValue<config, selectData>, 'queryKey' | 'queryFn'> | undefined;
        };
        type ReturnValue<config extends Config, selectData = getTotalPerSecond.ReturnValue> = RequiredBy<Query.QueryOptions<getTotalPerSecond.ReturnValue, Query.DefaultError, selectData, getTotalPerSecond.QueryKey<config>>, 'queryKey' | 'queryFn'>;
    }
}
/**
 * Gets the reward information for a specific account.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const info = await Actions.reward.getUserRewardInfo(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 *   account: '0xa5cc3c03994DB5b0d9A5eEdD10CabaB0813678AC',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The user's reward information (recipient, rewardPerToken, rewardBalance).
 */
export declare function getUserRewardInfo<config extends Config>(config: config, parameters: getUserRewardInfo.Parameters<config>): Promise<{
    rewardRecipient: `0x${string}`;
    rewardPerToken: bigint;
    rewardBalance: bigint;
}>;
export declare namespace getUserRewardInfo {
    type Parameters<config extends Config> = ChainIdParameter<config> & viem_Actions.getUserRewardInfo.Parameters;
    type ReturnValue = viem_Actions.getUserRewardInfo.ReturnValue;
    function queryKey<config extends Config>(parameters: Parameters<config>): readonly ["getUserRewardInfo", Parameters<config>];
    type QueryKey<config extends Config> = ReturnType<typeof queryKey<config>>;
    function queryOptions<config extends Config, selectData = ReturnValue>(config: Config, parameters: queryOptions.Parameters<config, selectData>): queryOptions.ReturnValue<config, selectData>;
    namespace queryOptions {
        type Parameters<config extends Config, selectData = getUserRewardInfo.ReturnValue> = getUserRewardInfo.Parameters<config> & {
            query?: Omit<ReturnValue<config, selectData>, 'queryKey' | 'queryFn'> | undefined;
        };
        type ReturnValue<config extends Config, selectData = getUserRewardInfo.ReturnValue> = RequiredBy<Query.QueryOptions<getUserRewardInfo.ReturnValue, Query.DefaultError, selectData, getUserRewardInfo.QueryKey<config>>, 'queryKey' | 'queryFn'>;
    }
}
/**
 * Sets or changes the reward recipient for a token holder.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const hash = await Actions.reward.setRecipient(config, {
 *   recipient: '0xa5cc3c03994DB5b0d9A5eEdD10CabaB0813678AC',
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction hash.
 */
export declare function setRecipient<config extends Config>(config: config, parameters: setRecipient.Parameters<config>): Promise<viem_Actions.setRecipient.ReturnValue>;
export declare namespace setRecipient {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.setRecipient.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.setRecipient.ReturnValue;
    type ErrorType = viem_Actions.setRecipient.ErrorType;
}
/**
 * Sets or changes the reward recipient for a token holder and waits for confirmation.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const result = await Actions.reward.setRecipientSync(config, {
 *   recipient: '0xa5cc3c03994DB5b0d9A5eEdD10CabaB0813678AC',
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction receipt and event data.
 */
export declare function setRecipientSync<config extends Config>(config: config, parameters: setRecipientSync.Parameters<config>): Promise<viem_Actions.setRecipientSync.ReturnValue>;
export declare namespace setRecipientSync {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.setRecipientSync.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.setRecipientSync.ReturnValue;
    type ErrorType = viem_Actions.setRecipientSync.ErrorType;
}
/**
 * Starts a new reward stream that distributes tokens to opted-in holders.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const hash = await Actions.reward.start(config, {
 *   amount: 100000000000000000000n,
 *   seconds: 86400,
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction hash.
 */
export declare function start<config extends Config>(config: config, parameters: start.Parameters<config>): Promise<viem_Actions.start.ReturnValue>;
export declare namespace start {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.start.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.start.ReturnValue;
    type ErrorType = viem_Actions.start.ErrorType;
}
/**
 * Starts a new reward stream that distributes tokens to opted-in holders and waits for confirmation.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const result = await Actions.reward.startSync(config, {
 *   amount: 100000000000000000000n,
 *   seconds: 86400,
 *   token: '0x20c0000000000000000000000000000000000001',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The transaction receipt and event data.
 */
export declare function startSync<config extends Config>(config: config, parameters: startSync.Parameters<config>): Promise<viem_Actions.startSync.ReturnValue>;
export declare namespace startSync {
    type Parameters<config extends Config = Config> = ChainIdParameter<config> & ConnectorParameter & UnionOmit<viem_Actions.startSync.Parameters<config['chains'][number], Account>, 'chain'>;
    type ReturnValue = viem_Actions.startSync.ReturnValue;
    type ErrorType = viem_Actions.startSync.ErrorType;
}
/**
 * Watches for reward scheduled events.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const unwatch = Actions.reward.watchRewardScheduled(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 *   onRewardScheduled: (args, log) => {
 *     console.log('Reward scheduled:', args)
 *   },
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns A function to unsubscribe from the event.
 */
export declare function watchRewardScheduled<config extends Config>(config: config, parameters: watchRewardScheduled.Parameters<config>): import("viem").WatchContractEventReturnType;
export declare namespace watchRewardScheduled {
    type Parameters<config extends Config> = ChainIdParameter<config> & viem_Actions.watchRewardScheduled.Parameters;
}
/**
 * Watches for reward recipient set events.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from 'tempo.ts/chains'
 * import { Actions } from 'tempo.ts/wagmi'
 *
 * const config = createConfig({
 *   chains: [tempo({ feeToken: '0x20c0000000000000000000000000000000000001' })],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const unwatch = Actions.reward.watchRewardRecipientSet(config, {
 *   token: '0x20c0000000000000000000000000000000000001',
 *   onRewardRecipientSet: (args, log) => {
 *     console.log('Reward recipient set:', args)
 *   },
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns A function to unsubscribe from the event.
 */
export declare function watchRewardRecipientSet<config extends Config>(config: config, parameters: watchRewardRecipientSet.Parameters<config>): import("viem").WatchContractEventReturnType;
export declare namespace watchRewardRecipientSet {
    type Parameters<config extends Config> = ChainIdParameter<config> & viem_Actions.watchRewardRecipientSet.Parameters;
}
//# sourceMappingURL=reward.d.ts.map